## IDF Component Manager Manifest File
dependencies:
  ## Required IDF version
  idf:
    version: ">=5.0.0"
  ## ESP32 Camera driver (provides esp_camera.h)
  espressif/esp32-camera: "~2.0.0"
  ## mDNS service discovery (auto-discover server on local network)
  espressif/mdns: "^1.2"
