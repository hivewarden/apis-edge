{
  "name": "apis-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "APIS E2E and API integration tests (Playwright)",
  "scripts": {
    "test:api": "npx playwright test --project=api",
    "test:e2e": "npx playwright test --project=e2e",
    "test:all": "npx playwright test",
    "test:api:p0": "npx playwright test --project=api --grep @P0",
    "test:e2e:p0": "npx playwright test --project=e2e --grep @P0",
    "test:report": "npx playwright show-report"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.3.0",
    "@playwright/test": "^1.58.2"
  }
}
